"use client";

import DashboardPageTemplate from "@/components/dashboard/DashboardPageTemplate";

export default function ManualStaysPage() {
    return (
        <DashboardPageTemplate
            title="Manual Stays"
            description="Manage manually added accommodation listings."
            actionLabel="Add Stay"
        />
    );
}
